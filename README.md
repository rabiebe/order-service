# order-service
Let’s implement a simple Spring Boot application using the Ports and Adapters pattern. The application will manage orders, with a REST API as the primary adapter and a database as the secondary adapter.
